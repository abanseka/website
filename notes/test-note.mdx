---
title: "Sample Note"
date: "2023-04-07"
description: "lorem ipsum dolor sit amet consectetur adipiscing elit"
---

#### An Image

![test image from pinterest](https://i.pinimg.com/564x/64/b0/29/64b029d1387ba506547e7a4d0b778fed.jpg)

---

# Test Page 🔥

### Code Block:

```ts title="/src/components/widgets/NavigationMenu"
const NavigationMenu = ({
  ma,
  className,
  renderIcon = true,
}: MenuWidgetProps) => {
  const navLinks: navLink[] = [
    { id: 1, label: "Notes", href: "/", icon: <JournalIcon /> },
    { id: 2, label: "Works", href: "/works", icon: <WorkIcon /> },
  ];

  const socialLinks: navLink[] = [
    {
      id: 3,
      label: "GitHub",
      href: "https://github.com/abanseka",
      icon: <GitHub />,
    },
    {
      id: 4,
      label: "LinkedIn",
      href: "https://www.linkedin.com/in/abanseka-sylvester/",
      icon: <Person />,
    },
  ];

  return <MenuContainer> {/*code in here*/}</MenuContainer>;
};
```

### An Un-Ordered List:

- some random points in a list: point 1
- some random points in a list: point 2
- some random points in a list: point 3

### An Ordered List:

1. One
2. Two
3. Three

### Nested List | UL:

- This is a Regular List
  - this is a nested List
    - within a nested List 🏀

Regular text with <s> _emphasis_</s> _Italic_ and **bold** text 😜. here's a
nice [link]("#") that'll lead to a Dead End! 💀. so the trick is to just write
on one line and the <s>line break</s> margins will handle the rest.

to add a second paragraph just skip one line, and continue _typing_ whatever it
is you had to type and it'll be **automatically escaped** to the next line 🌈
